# Task 11 — Meal plan

**Data:** 2026-06-16
**Commit:** `feat: meal plan repository and hooks (pranzo + cena per giorno)`

## Note schema

Aggiornato dopo feedback utente: ogni giorno ha **due pasti** (pranzo + cena).

- `MealType = 0 | 1` aggiunto a `src/db/types.ts`
- Schema Dexie aggiornato a **versione 2**: compound index `[isoWeek+dayIndex+mealType]`

## File creati/modificati

### `src/db/types.ts`
- `MealType = 0 | 1` (0=pranzo, 1=cena)
- `MealPlan.mealType: MealType` aggiunto

### `src/db/db.ts`
- v1: schema base (mealPlans senza compound index)
- v2: aggiunge `[isoWeek+dayIndex+mealType]` per upsert preciso

### `src/db/repositories/mealPlan.ts`
- `MealPlanDay` — dayIndex + pranzo/cena separati con rispettivi id
- `getMealPlan(isoWeek)` → 7 giorni × 2 pasti
- `upsertMealPlan(isoWeek, dayIndex, mealType, dish)` — crea/aggiorna/elimina
- `clearMealPlan(isoWeek)`, `getMealDishes(isoWeek)`

### `src/hooks/useMealPlan.ts`
- `useMealPlan(isoWeek)`, `useUpdateMealSlot(isoWeek)`, `useClearMealPlan(isoWeek)`
