# Task 10 — Storico prezzi e statistiche supermercato

**Data:** 2026-06-16
**Commit:** `feat: price history repository and stats hooks`

## File creati

### `src/db/repositories/priceHistory.ts`
- `PricePoint` — sessionId, supermarketName, priceCents, date
- `ItemPriceStats` — avg/min/max/buyCount/last + history[]
- `getItemPriceHistory(itemId)` — tutti i prezzi pagati, ordinati per data, con stats
- `StoreItemPrice` — supermarketId, name, bestPriceCents, purchaseCount
- `getItemPriceByStore(itemId)` — miglior prezzo per supermercato

### `src/hooks/usePriceHistory.ts`
- `useItemPriceHistory(itemId)` — stats + storia prezzi item
- `useItemPriceByStore(itemId)` — prezzi per supermercato
- `useSupermarketStats()` — statistiche aggregate per tutti i supermercati
