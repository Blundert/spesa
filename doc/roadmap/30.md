# Task 30 — Installa PWA con tutorial (@khmyznikov/pwa-install)

**Data:** 2026-06-17
**Commit:** `feat: PWA install tutorial via @khmyznikov/pwa-install`

Parte 4/5 del blocco Impostazioni. Su scelta dell'utente si usa la libreria web-component
(cross-browser, incl. istruzioni iOS) invece di una soluzione custom.

## Implementato

### Dipendenza
- `@khmyznikov/pwa-install` (web component; usa `lit`/`@lit/react`, già risolti come transitivi).
  Wrapper React: `@khmyznikov/pwa-install/react-legacy`.

### Montaggio (`src/main.tsx`)
- `<PWAInstall manualApple manualChrome useLocalStorage />` montato app-wide (early) per catturare
  `beforeinstallprompt`. Modalità **manuale**: nessun popup automatico, si apre solo da Impostazioni.
- Rimosso il vecchio banner custom `InstallPrompt.tsx` (sostituito; evita doppia gestione di
  `beforeinstallprompt`).

### Impostazioni (`src/screens/ImpostazioniScreen.tsx`)
- Riga **"Installa l'app"** → `document.querySelector<PWAInstallElement>('pwa-install')?.showDialog(true)`:
  apre il dialog/tutorial della libreria (su iOS mostra i passi Condividi → Aggiungi a Home; su
  Android/desktop usa il prompt nativo se disponibile). Etichetta da `install.title`.

## Verifica
- `tsc -b`: zero errori. `npm run build`: completato (web component + lit nel bundle).
- `npm run lint`: zero errori NUOVI (restano i 2 preesistenti `set-state-in-effect`).

## Note / da verificare a runtime
- Chrome desktop/Android: la riga apre il dialog con il pulsante di installazione nativo (se
  l'app è installabile).
- iOS Safari: il dialog mostra le istruzioni manuali.
- App già installata (standalone): il dialog si comporta di conseguenza.
- `install.subtitle`/`install.install` restano nei dizionari (non più usati dopo la rimozione del banner).
