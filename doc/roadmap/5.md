# Task 5 — QueryClient + repository Dexie + convenzione query key

**Data:** 2026-06-16
**Commit:** `feat: add tanstack query client, query keys, and base repository`

## File creati/modificati

### `src/lib/queryClient.ts`
- `QueryClient` con `staleTime: Infinity` (dati locali, nessun refetch automatico)
- `gcTime: 1h`, `retry: false`

### `src/db/queryKeys.ts`
- Oggetto `qk` con funzioni typed per ogni dominio
- Struttura `[domain, ...params]` per invalidazione granulare
- Es: `qk.listItems('2026-W24')` → `['listItems', '2026-W24']`

### `src/db/repositories/categories.ts`
- `getCategories()` — tutte le categorie ordinate per sortOrder
- `getCategoryById(id)` — singola categoria

### `src/main.tsx`
- Aggiunto `QueryClientProvider` wrappa `RouterProvider`

## Convenzione query key

```
categories            → ['categories']
items                 → ['items']
items/:id             → ['items', id]
supermarkets          → ['supermarkets']
weekBudget/:week      → ['weekBudget', isoWeek]
listItems/:week       → ['listItems', isoWeek]
sessions/:week        → ['sessions', isoWeek]
sessions (all)        → ['sessions']
session/:id           → ['sessions', 'detail', id]
purchases/:sessionId  → ['purchases', sessionId]
priceHistory/:itemId  → ['priceHistory', itemId]
mealPlan/:week        → ['mealPlan', isoWeek]
```
