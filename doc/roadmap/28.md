# Task 28 — Pagina Impostazioni + cambio lingua + versione

**Data:** 2026-06-17
**Commit:** `feat: settings page with language switch and app version`

Parte 2/5 del blocco Impostazioni.

## Implementato

### Versione app da build
- `vite.config.ts`: legge `package.json` via `node:fs` (niente resolveJsonModule) e definisce
  `__APP_VERSION__` con `define`.
- `src/global.d.ts`: `declare const __APP_VERSION__: string`.

### Pagina Impostazioni (`src/screens/ImpostazioniScreen.tsx`)
- Route shell `/impostazioni` (`src/router.ts`) + voce "Impostazioni" nella side nav (`AppShell`).
- Sezione **Lingua**: due righe Italiano/English con check sull'attiva → `i18n.changeLanguage(lng)`
  + `localStorage['app-language']`. Il cambio è immediato (tutta l'app ri-renderizza via i18n) e
  persiste al reload.
- Sezione **Versione**: mostra `__APP_VERSION__` (da package.json).
- Chiavi `settings.*` aggiunte ai dizionari it/en.

## Verifica
- `tsc -b`: zero errori. `npm run build`: completato (define sostituito a build).
- `npm run lint`: zero errori NUOVI (restano i 2 preesistenti `set-state-in-effect`).

## Note / da verificare a runtime
- Impostazioni → English: tutta l'interfaccia e le date passano in inglese; reload mantiene la scelta.
- La versione mostrata è quella di package.json (attualmente "0.0.0").
- Sezioni "Installa" (R10) e "Azzera dati" (R9) verranno aggiunte alla stessa pagina.
