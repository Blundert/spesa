# Task R9 — Azzera tutti i dati + drawer di conferma

**Data:** 2026-06-17
**Commit:** `feat: clear all data from settings with confirm drawer`

Parte 3/5 del blocco Impostazioni.

## Implementato

### Wipe dati (`src/db/db.ts`)
- `wipeAllData()`: transazione `rw` su tutte le tabelle → `clear()` di items, supermarkets,
  weekBudgets, listItems, sessions, purchases, mealPlans e categories, poi re-inserisce
  `SEED_CATEGORIES`. Le categorie restano (struttura, non dati utente).

### Impostazioni (`src/screens/ImpostazioniScreen.tsx`)
- Sezione **Dati** con bottone distruttivo "Azzera tutti i dati" (rosso) → apre un `BottomSheet`
  (Vaul, esce dal basso) di conferma: titolo + avviso, "Elimina tutto" (rosso) e "Annulla".
- Conferma → `wipeAllData()` + `queryClient.clear()` (invalida tutta la cache) + toast.
- Chiavi `settings.data/deleteAll/confirmTitle/confirmBody/deleteConfirm/cleared` nei dizionari;
  "Annulla" riusa `common.cancel`.

## Verifica
- `tsc -b`: zero errori. `npm run build`: completato.
- `npm run lint`: zero errori NUOVI (restano i 2 preesistenti `set-state-in-effect`).

## Note / da verificare a runtime
- Con dati presenti: Impostazioni → "Azzera tutti i dati" → drawer dal basso → "Elimina tutto":
  lista/storico/supermercati/budget vuoti, categorie ancora presenti, niente crash; "Annulla" chiude
  senza effetti.
- Colore rosso `#D14343` introdotto solo per l'azione distruttiva (palette altrove invariata).
