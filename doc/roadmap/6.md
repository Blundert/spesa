# Task 6 — Items e Supermarkets con dedup su normalizedName

**Data:** 2026-06-16
**Commit:** `feat: items and supermarkets repositories with dedup`

## File creati

### `src/lib/normalize.ts`
- `normalizeName(name)` — lowercase + trim + collassa spazi: usato per dedup

### `src/db/repositories/items.ts`
- `getItems()`, `getItemById(id)`, `getItemsByCategory(categoryId)`
- `findItemByName(name)` — cerca per normalizedName
- `upsertItem(name, categoryId, suggestedPriceCents?)` — crea solo se non esiste, ritorna id
- `updateItemPrices(id, lastPriceCents, suggestedPriceCents)` — aggiorna dopo acquisto
- `deleteItem(id)` — cancella item + listItems associati (transazione)

### `src/db/repositories/supermarkets.ts`
- `getSupermarkets()`, `getSupermarketById(id)`
- `upsertSupermarket(name)` — dedup su normalizedName
- `deleteSupermarket(id)`
- `getSupermarketStats()` → `{ supermarket, sessionCount, totalSpentCents }[]`
