# Task R12 — Deploy GitHub Pages (dominio spesa.mattegranzotto.com)

**Data:** 2026-06-17
**Commit:** `ci: deploy to GitHub Pages with custom domain and SPA fallback`

## Implementato (repo)

- `.github/workflows/deploy.yml`: build + deploy via GitHub Actions (Node 22, `npm ci` +
  `npm run build`, `cp dist/index.html dist/404.html` per il fallback SPA, `configure-pages` +
  `upload-pages-artifact` + `deploy-pages`). Trigger su push `main` + `workflow_dispatch`.
- `public/CNAME` = `spesa.mattegranzotto.com` (copiato in `dist/`, mantiene il dominio custom).
- `vite.config.ts`: `workbox.navigateFallback: 'index.html'` → il SW serve l'app shell per le
  navigazioni SPA (anche offline). `base` invariato (`/`, corretto per dominio alla root).

## Verifica
- `npm run build` OK; `dist/` contiene `index.html`, `404.html` (dopo cp, fatto dal workflow in CI),
  `CNAME`, `manifest.webmanifest`, `sw.js` (con `createHandlerBoundToURL("index.html")`), icone e
  `apple-splash-*`.
- `tsc -b` zero errori; `npm run lint` invariato (2 preesistenti `set-state-in-effect`).

## Passi manuali (utente)
1. Crea repo pubblico `blundert/spesa` (nome libero).
2. `git remote add origin https://github.com/blundert/spesa.git` → `git push -u origin main`.
3. Settings → Pages → Source = "GitHub Actions".
4. Settings → Pages → Custom domain = `spesa.mattegranzotto.com` → Save → poi Enforce HTTPS.
5. DNS di mattegranzotto.com: record CNAME `spesa` → `blundert.github.io.`.

## Note
- TLS GitHub: da minuti a ~24h dopo DNS+dominio; "Enforce HTTPS" attivabile a cert pronto.
- Repo privato richiederebbe GitHub Pro per Pages → si usa repo pubblico.
- sharp (assets-generator) gira in CI con i binari prebuilt di `npm ci` su ubuntu-latest.
