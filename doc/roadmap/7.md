# Task 7 — Week e budget: selettore rimanente derivato

**Data:** 2026-06-16
**Commit:** `feat: week budget, sessions, purchases repositories and budget selectors`

## File creati

### `src/db/repositories/weekBudget.ts`
- `getWeekBudget(isoWeek)` — legge o crea il budget default (5 buoni × €8,00)
- `updateWeekBudget(isoWeek, buoniCount, buoniValueCents)` — upsert

### `src/db/repositories/sessions.ts`
- `getSessions()` — tutte, ordinate per data decrescente
- `getSessionsByWeek(isoWeek)`, `getSessionById(id)`
- `createSession(isoWeek, supermarketId)` → id
- `finishSession(id)` — imposta finishedAt
- `deleteSession(id)` — transazione: cancella anche i purchases

### `src/db/repositories/purchases.ts`
- `getPurchasesBySession(sessionId)`, `addPurchase(...)`, `updatePurchasePrice/Quantity()`
- `removePurchase(id)`, `sessionTotalCents(sessionId)`

### `src/lib/budgetSelectors.ts`
- `BudgetSummary`: totalCents, spentCents, remainingCents, outOfPocketCents, isOver
- `computeBudgetSummary(budget, sessions, purchases)` — considera solo sessioni finite
- `computeLiveBudgetSummary(budget, purchases)` — include sessione corrente (live mode)
