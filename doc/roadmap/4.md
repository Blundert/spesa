# Task 4 — Database Dexie: schema, tipi entità, seed categorie

**Data:** 2026-06-16
**Commit:** `feat: add dexie schema, entity types, and category seed`

## File creati

### `src/db/types.ts`
Tipi TypeScript puri (nessuna dipendenza da Dexie):
- `Category` — id, name, sortOrder
- `Item` — name, normalizedName, categoryId, lastPriceCents, suggestedPriceCents
- `Supermarket` — name, normalizedName
- `WeekBudget` — isoWeek (unique), buoniCount, buoniValueCents
- `ListItem` — isoWeek, itemId, quantity, addedAt
- `Session` — isoWeek, supermarketId, startedAt, finishedAt
- `Purchase` — sessionId, itemId, priceCents, quantity
- `MealPlan` — isoWeek, dayIndex (0=Lun … 6=Dom), dish

### `src/db/db.ts`
- `SpesaDb extends Dexie` con versione 1
- Indici ottimizzati: compound `[isoWeek+itemId]`, `[isoWeek+dayIndex]`, `[sessionId+itemId]`
- `isoWeek` unique su `weekBudgets` (una sola configurazione per settimana)
- `db.on('populate')` — seed automatico delle 6 categorie al primo avvio

## Categorie seed
```
0  Frutta e verdura
1  Frigo
2  Dispensa
3  Panetteria
4  Bevande
5  Altro
```
