# Task 20 — PWA Polish

**Data:** 2026-06-16
**Commit:** `feat: PWA polish`

## Implementato

### shouldScaleBackground su Vaul
- `BottomSheet.tsx`: aggiunto `shouldScaleBackground` a `Drawer.Root`
- `PriceKeypad.tsx`: aggiunto `shouldScaleBackground` a `Drawer.Root`
- Effetto iOS nativo: l'app-shell scala e retrocede quando un drawer è aperto

### InstallPrompt
- `src/components/InstallPrompt.tsx`: banner fisso sopra la bottom bar
- Usa evento `beforeinstallprompt` per mostrare il banner solo quando il browser lo supporta (Chrome/Edge Android, non iOS)
- "Installa" chiama `prompt()` e attende `userChoice`
- "×" persiste `pwa-install-dismissed=1` in `localStorage` → non ricompare
- Montato in `main.tsx` fuori dal router, sempre disponibile

### Service Worker
- Build confermato pulito: `dist/sw.js` + `dist/workbox-835c8c05.js` generati
- 21 entry precachate (595 KiB), app shell funzionante offline
- Strategia `generateSW` + `autoUpdate` da Task 2

### Build finale
- `tsc -b` zero errori
- `vite build` completato, solo warning non bloccanti su chunk size e dynamic import ridondante
