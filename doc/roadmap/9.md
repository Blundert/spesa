# Task 9 — Sessioni e acquisti: purchase mutation

**Data:** 2026-06-16
**Commit:** `feat: shopping session and purchase hooks`

## File creati

### `src/hooks/useShopping.ts`
- `useWeekBudget(isoWeek)`, `useUpdateWeekBudget(isoWeek)`
- `useSessionsByWeek(isoWeek)`, `useAllSessions()`
- `useCreateSession(isoWeek)`, `useFinishSession(isoWeek)`
- `usePurchasesBySession(sessionId)`
- `useAddPurchase(isoWeek)`:
  - Aggiunge l'acquisto con quantity default 1
  - Aggiorna `lastPriceCents` e `suggestedPriceCents` (media mobile) sull'item
- `useUpdatePurchase(sessionId)` — aggiorna price e/o quantity
- `useRemovePurchase(sessionId)`
