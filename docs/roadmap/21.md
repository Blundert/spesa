# Task 21 — Schema Dexie v3 + lista attiva globale

**Data:** 2026-06-16
**Commit:** `refactor: make shopping list global, add Session.confirmedTotalCents (Dexie v3)`

Parte 1/5 della rework dei concetti (settimana ↔ pasti, home recap, spesa ↔ lista).
Schema e lista globale sono accorpati in un unico commit perché togliere `ListItem.isoWeek`
rompe contemporaneamente repo, hook e tutti i call site: non sono separabili compilando.

## Implementato

### Tipi (`src/db/types.ts`)
- `ListItem`: rimosso `isoWeek`. Ora `{ id?, itemId, quantity, addedAt }` — la lista è unica
  e globale, si svuota a fine spesa.
- `Session`: aggiunto `confirmedTotalCents: number | null` (importo confermato a fine spesa;
  null finché non confermato). Sarà autoritativo per il recap in R2.

### Schema (`src/db/db.ts`)
- Nuova `this.version(3)`:
  - `listItems: '++id, itemId'` (rimosso `isoWeek` e il compound `[isoWeek+itemId]`)
  - `sessions: '++id, isoWeek, supermarketId, startedAt'` (ribadito invariato; `.stores()`
    ridefinisce per intero la tabella)
  - `weekBudgets`/`mealPlans` omessi → invariati
- `.upgrade()`:
  - `tx.table('listItems').clear()` — le voci di lista sono effimere
  - backfill esplicito `confirmedTotalCents = null` su tutte le sessioni esistenti via
    `toCollection().modify()` (così il tipo `number|null` resta veritiero)

### Repository (`src/db/repositories/listItems.ts`)
- Tutte le firme senza `isoWeek`: `getListItems()`, `addToList(itemId)` (dedup su `itemId`),
  `updateListItemQuantity`, `removeFromList`, `clearList()` (`db.listItems.clear()`).
- `src/db/repositories/sessions.ts`: `createSession` ora inserisce `confirmedTotalCents: null`
  (necessario per compilare; il comportamento di `finishSession` cambierà in R2).

### Query key (`src/db/queryKeys.ts`)
- `listItems: () => ['listItems']` (senza arg).
- Aggiunta `purchasesForWeek: (isoWeek) => ['purchasesForWeek', isoWeek]`: centralizza la chiave
  ad-hoc finora inline in Home/Lista/AppShell (serve per l'invalidazione a fine spesa in R2).

### Hook (`src/hooks/useListItems.ts`)
- Tutti gli hook senza `isoWeek`: `useListItems()`, `useAddNewItemToList(defaultCategoryId)`,
  `useAddToList()`, `useRemoveFromList()`, `useUpdateListQuantity()`, `useClearList()`.

### Call site aggiornati
- `HomeScreen.tsx`, `ListaScreen.tsx`, `SpesaScreen.tsx`, `AppShell.tsx`:
  `useListItems()` senza arg; le `usePurchasesForWeek` inline ora usano `qk.purchasesForWeek(isoWeek)`.

## Verifica
- `tsc -b`: zero errori.
- `npm run build`: completato (`✓ built`), solo warning preesistenti (chunk size / dynamic import).
- `npm run lint`: zero errori NUOVI. Restano 2 errori **preesistenti** su HEAD
  (`react-hooks/set-state-in-effect` in `PriceKeypad.tsx:28` e `SpesaScreen.tsx:65`), non toccati
  da questa task e fuori scope.

## Note / da verificare a runtime
- La migrazione v3 azzera la lista esistente (scelta voluta: liste effimere) — da confermare
  in browser su DB con dati pregressi (nessun crash, sessioni passate intatte).
- Comportamento funzionale invariato per l'utente in R1: la spesa finisce ancora senza conferma
  (cambia in R2); la lista non è più scoped per settimana.
