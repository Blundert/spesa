# Task 37 — Test di unità, coverage 100%

## Obiettivo

Aggiungere test di unità per tutti i moduli puri in `src/lib/` con copertura 100% su
statements, branches, functions e lines.

## Cosa è stato fatto

- Installato `@vitest/coverage-v8`
- Aggiornato `vitest.config.ts`: aggiunta sezione `coverage` con provider v8, `include`
  esplicito sui 6 file target e soglie al 100%
- Aggiunto script `"coverage"` in `package.json`
- Creati 6 nuovi file di test:
  - `src/lib/money.test.ts` — toCents, fromCents, formatCents, formatCentsPlain, addCents, clampCents
  - `src/lib/normalize.test.ts` — normalizeName
  - `src/lib/date.test.ts` — ISO week, weekRange, shiftISOWeek, dayShort/Full, formatWeekLabel/ShortDate/LongDate
    (i18n mockato con `vi.hoisted` + `vi.mock`)
  - `src/lib/budgetSelectors.test.ts` — weekSpendSummary, liveSpendSummary (tutti i branch)
  - `src/lib/debugFlag.test.ts` — getDebugViewport, setDebugViewport, onDebugViewportChange
    (localStorage/window/Event stubatti con `vi.stubGlobal` in beforeEach)
  - `src/lib/imageUtils.test.ts` — compressImage (Image/document/URL.createObjectURL mockati)
- Rimosso `|| 7` irraggiungibile in `shiftISOWeek` (d è sempre lunedì → dead branch)

## Risultato

```
Statements : 100% (98/98)
Branches   : 100% (27/27)
Functions  : 100% (29/29)
Lines      : 100% (95/95)
```

71 test totali, tutti verdi.
