# Task 18 — Dettaglio oggetto (storico prezzi)

**Data:** 2026-06-16
**Commit:** `feat: item detail screen with price history and sparkline`

## Implementato in `src/screens/ItemDetailScreen.tsx`

### Layout
- Header: nome item + back → /lista
- Card prezzo medio (grande) + ultimo prezzo
- Sparkline SVG inline calcolato dai punti storia
- Tre mini-card: MIN / MAX / ACQUISTI
- Lista "Per supermercato": miglior prezzo + barra progresso proporzionale
- "Storico prezzi": ogni acquisto con store + data + prezzo (ordine decrescente)
- Stato vuoto se nessun acquisto registrato

### Dati
- `useItemPriceHistory(itemId)` — avg/min/max/last + history
- `useItemPriceByStore(itemId)` — per barre comparative
