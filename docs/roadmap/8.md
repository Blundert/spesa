# Task 8 — Lista della spesa: query e mutation

**Data:** 2026-06-16
**Commit:** `feat: shopping list repository and tanstack query hooks`

## File creati

### `src/db/repositories/listItems.ts`
- `ListItemWithItem` — join ListItem + Item (name, categoryId, suggestedPriceCents)
- `getListItems(isoWeek)` — query con join
- `addToList(isoWeek, itemId)` — se esiste già incrementa quantity, altrimenti aggiunge
- `removeFromList(id)`, `updateListItemQuantity(id, q)` — se q≤0 rimuove
- `clearList(isoWeek)` — svuota la lista

### `src/hooks/useListItems.ts`
- `useListItems(isoWeek)` — query
- `useAddToList(isoWeek)` — mutation, invalida list
- `useAddNewItemToList(isoWeek, defaultCategoryId)` — upsertItem + addToList in una mutation
- `useRemoveFromList(isoWeek)`, `useUpdateListQuantity(isoWeek)`, `useClearList(isoWeek)`
