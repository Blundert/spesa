# Task 33 — Menu di navigazione dal basso (rimosso drawer laterale)

**Data:** 2026-06-17
**Commit:** `feat: bottom-sheet nav menu, drop side drawer`

Il drawer laterale con effetto push (aperto dall'hamburger in alto a destra) era scomodo.
Sostituito con un bottone accanto alla barra in basso che apre un bottom sheet col menu.

## Implementato (`src/components/AppShell.tsx`)
- **Rimossi**: side nav con effetto push (`translateX/scale`), bottone hamburger in alto a destra,
  overlay di chiusura, stato `navOpen`, `handleNavShopGo` e il componente `NavItem`.
- La barra in basso ora è una **riga flex**: a sinistra il box riepilogo (apre il foglio budget,
  invariato), a destra un **bottone scuro** (`w-[60px]`, icona menu) che apre il menu.
- Nuovo componente **`MenuSheet`** (riusa `BottomSheet`): voci Settimana / Lista / Storico /
  Supermercati / Impostazioni, con check sulla voce attiva (via `pathname`); il tap naviga e chiude.
- Chiave `nav.menu` ("Menu") aggiunta ai dizionari it/en.

## Verifica
- `tsc -b`: zero errori. `npm run build`: completato.
- `npm run lint`: zero errori NUOVI (restano i 2 preesistenti `set-state-in-effect`).

## Note / runtime
- Niente più hamburger in alto: la navigazione passa dal bottone accanto alla barra in basso.
- Il bottom sheet del menu non usa lo scale (coerente con la fix precedente): nessun nero.
