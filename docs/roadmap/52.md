# Task 52 — Fix grafico andamento settimanale (v0.20.3 – v0.20.5)

## Cosa è stato fatto

### v0.20.3
- Grafico "Andamento" in Statistiche: importo in euro sopra ogni barra e data in formato G/M sotto.

### v0.20.4
- Fix rebucket: le sessioni vengono riposizionate nel grafico in base al `weekStartDay` attualmente configurato, non a quello in vigore al momento del salvataggio. La logica `sessionWeekKey()` in `stats.ts` ricalcola la settimana di appartenenza da `startedAt`.
- Test aggiornati: `WEEK` corretto a `'2026-06-22'` (lunedì), `WEEK_TS` introdotto per timestamp realistici, test di rebucket aggiunto.

### v0.20.5
- Il grafico non mostra più le settimane iniziali vuote (barre grigie a sinistra): trimming nel componente `WeeklyBarChart` via `findIndex` + `slice`.

## File modificati
- `src/db/repositories/stats.ts`
- `src/db/repositories/stats.test.ts`
- `src/hooks/useStats.ts`
- `src/screens/StatisticheScreen.tsx`
- `src/data/changelog.ts`
- `package.json`
