# Task 14 — Home / settimana

**Data:** 2026-06-16
**Commit:** `feat: home week screen with budget hero and budget sheet`

## Implementato in `src/screens/HomeScreen.tsx`

### Layout
- `AppLayout` wrappa tutto (side nav + floating bottom bar)
- Header: "La mia settimana" + label settimana (es. "9 – 15 giu")
- Hero dinamico: grande numero rimanente (positivo) o "Da pagare a parte" (negativo, sfondo scuro)
- Stats strip: SPESO / OGGETTI / BUDGET in card bianca
- Azioni: link a /pasti e /lista con icone
- CTA "Inizia la spesa" → /spesa

### BudgetSheet (BottomSheet Vaul)
- Aperto dal tap sulla bottom bar
- Stepper buoni pasto (min 1, max 20) e valore unitario (step €0,50)
- Mostra budget totale calcolato
- Link rapidi: /spesa, /pasti, /lista

### Dati
- `useWeekBudget`, `useSessionsByWeek`, `usePurchasesForWeek` (query locale su db)
- `computeBudgetSummary` per hero + stats
- `useUpdateWeekBudget` per stepper
