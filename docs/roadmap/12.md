# Task 12 — Sonner, Vaul, tastierino prezzo+quantità, layout, navigazione

**Data:** 2026-06-16
**Commit:** `feat: UI primitives — AppLayout, PriceKeypad, BottomSheet, full router`

## Librerie installate
- `sonner` — toast notifications
- `vaul` — bottom sheet/drawer

## File creati/modificati

### `src/main.tsx`
- `<Toaster>` Sonner: position=bottom-center, offset=108px (sopra la bottom bar), style dark (#2A2A2C)

### `src/components/AppLayout.tsx`
- Side nav push (effetto Vaul manuale: translateX+scale del main content)
- Nav items: Settimana, Lista, Storico, Supermercati + "Inizia la spesa" CTA
- Floating bottom bar: mostra rimanente/da pagare, speso, conteggio oggetti
- Menu hamburger in alto a destra, overlay per chiudere nav

### `src/components/PriceKeypad.tsx`
- Bottom sheet Vaul con tastierino numerico (12 tasti: 1-9, 00, 0, ⌫)
- Display centesimi: €X,XX aggiornato live
- `initialCents` per pre-popolare (modifica prezzo)
- `onConfirm(cents)` callback

### `src/components/BottomSheet.tsx`
- Wrapper generico Vaul per sheet multipli
- Handle drag, overlay, safe-area-inset-bottom

### `src/router.ts`
- Route complete: /, /spesa, /lista, /storico, /storico/:sessionId, /supermercati, /pasti, /item/:itemId
- Placeholder screen per task 13-19

## Note Vaul
- `Drawer.Root` + `Drawer.Portal` + `Drawer.Overlay` + `Drawer.Content` + `Drawer.Handle`
- `dismissible`: si chiude con swipe o tap overlay
- La prop `shouldScaleBackground` non è presente nella versione corrente installata

## Note Sonner
- `toast('msg', { description })` per toast con sottotitolo
- `toast.success()`, `toast.error()` per varianti
